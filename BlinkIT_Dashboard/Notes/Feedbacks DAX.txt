	Total_Feedbacks = DISTINCTCOUNT(Master_Table[feedback_id])

	Period_Feedbacks = 
		var selected_Period = [Selected_Period]
		var Period_Value = CALCULATE([Total_Feedbacks],'Calendar'[MonthIndex]<=selected_Period)
		RETURN if(selected_Period=0,[YTD_Feedbacks],Period_Value)

	YTD_Feedbacks = var YTDSysMonth = [YTDMonth] 
		RETURN
		CALCULATE([Total_Feedbacks],'Calendar'[MonthIndex]<=YTDSysMonth)

	Rating_Label = "Rating "&Master_Table[rating]